---
subcategory : "Instances"
---

# ovh_cloud_instance_group

Creates an instance group (placement group) in a public cloud project.

~> **WARNING** An instance group is **immutable**: it has no update route. Changing any attribute (`name`, `region`, `policy`) forces the group to be destroyed and re-created.

~> **NOTE** Membership is set only at instance-creation time through the instance's `group_id`; this resource never manages members.

## Example Usage

```terraform
resource "ovh_cloud_instance_group" "group" {
  service_name = <Public cloud project id>
  region       = "GRA11"
  name         = "my-instance-group"
  policy       = "ANTI_AFFINITY"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Instance group name
- `policy` (String) Placement policy applied to the group's member instances (`AFFINITY`, `ANTI_AFFINITY`)
- `region` (String) Region where the instance group will be created
- `service_name` (String) Service name of the resource representing the id of the cloud project

### Read-Only

- `checksum` (String) Computed hash representing the current target specification value
- `created_at` (String) Creation date of the instance group
- `current_state` (Attributes) Current state of the instance group (see [below for nested schema](#nestedatt--current_state))
- `id` (String) Instance group ID
- `resource_status` (String) Instance group readiness in the system (`CREATING`, `DELETING`, `ERROR`, `READY`)
- `updated_at` (String) Last update date of the instance group

<a id="nestedatt--current_state"></a>
### Nested Schema for `current_state`

Read-Only:

- `location` (Attributes) Location details (see [below for nested schema](#nestedatt--current_state--location))
- `members` (Attributes List) Instances currently belonging to the group (see [below for nested schema](#nestedatt--current_state--members))
- `name` (String) Instance group name
- `policy` (String) Placement policy

<a id="nestedatt--current_state--location"></a>
### Nested Schema for `current_state.location`

Read-Only:

- `availability_zone` (String) Availability zone
- `region` (String) Region


<a id="nestedatt--current_state--members"></a>
### Nested Schema for `current_state.members`

Read-Only:

- `id` (String) Instance ID





## Import

An instance group in a public cloud project can be imported using the `service_name` and `id` (identifier of the instance group) properties, separated by a `/`.

```bash
$ terraform import ovh_cloud_instance_group.group service_name/id
```
